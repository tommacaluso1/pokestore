{
  "buildCommand": "npx prisma migrate deploy --schema=packages/db/prisma/schema.prisma && npx turbo run build --filter=web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
